
version: '{branch}.{build}'

#cache:
#  - c:\build-cache
#  - c:\build-cache\sdk

environment:
  matrix:
    - APPVEYOR_BUILD_WORKER_IMAGE: Visual Studio 2017
      ARCHITECTURE: x64
      BUILD_CRT: VC15
#    - APPVEYOR_BUILD_WORKER_IMAGE: Visual Studio 2015
#      ARCHITECTURE: x86
#      BUILD_CRT: VC15

install:
  - appveyor\install.cmd

build_script:
  - appveyor\build.cmd

artifacts:
  - path: artifacts
    name: mod_authn_ntlm-%MOD_NTL_VERSION%-%ARCHITECTURE%-%BUILD_CRT%-%APPVEYOR_REPO_TAG_NAME%
    type: zip
